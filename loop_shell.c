#include <stdio.h>

void loop_shell(void)
{
int status;
char *commandLine;
char **arguments;

do {
	printf("$ ");
	commandLine = ();
	// args = lsh_split_line(line);
	// status = lsh_execute(args);

free(line);
}
}
