/*
#include <stdio.h>

void loop_shell(void)
{
int status;
char *commandLine;
char **arguments;

while (1)
{
	printf("$ ");
	commandLine = read_line();
	arguments = strtoken(commandLine);
	// status = lsh_execute(args);
}
free(commandLine);
}
*/